## Usage
```js
const a = require("geta");

let uppercase = false;
const lower = a.GetLetter(uppercase);

uppercase = true;
const upper = a.GetLetter(uppercase);

console.log(`Lower Case: ${lower} - Upper Case: ${upper}`);
```
